import React from "react";

import './CreateButton.css';

const CreateButton = () => {
    return (
        <button
          className= 'button'
          type="submit"
        >
          Create Task
        </button>
      );
}

export default CreateButton;