import React from 'react';

import './SubmitButton.css';

const SubmitButton = props => {
  return (
    <button
      className= 'button'
      type="submit"
    >
      Submit Task
    </button>
  );
};

export default SubmitButton;
